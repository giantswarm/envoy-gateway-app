diff --git a/vendor/gateway-helm/values.yaml b/helm/envoy-gateway/values.yaml
index 17a7dd8..aaeaa45 100644
--- a/vendor/gateway-helm/values.yaml
+++ b/helm/envoy-gateway/values.yaml
@@ -2,7 +2,8 @@
 global:
   # If set, these take highest precedence and change both envoyGateway and ratelimit's container registry and pull secrets.
   # -- Global override for image registry
-  imageRegistry: ""
+  image:
+    registry: "gsoci.azurecr.io"
   # -- Global override for image pull secrets
   imagePullSecrets: []
 
@@ -11,7 +12,7 @@ global:
   images:
     envoyGateway:
       # This is the full image name including the hub, repo, and tag.
-      image: docker.io/envoyproxy/gateway:v1.5.4
+      image: gsoci.azurecr.io/giantswarm/envoyproxy-gateway:v1.5.4
       # Specify image pull policy if default behavior isn't desired.
       # Default behavior: latest images will be Always else IfNotPresent.
       pullPolicy: IfNotPresent
@@ -19,19 +20,20 @@ global:
       pullSecrets: []
     ratelimit:
       # This is the full image name including the hub, repo, and tag.
-      image: "docker.io/envoyproxy/ratelimit:e74a664a"
+      image: "gsoci.azurecr.io/giantswarm/envoyproxy-ratelimit:e74a664a"
       # Specify image pull policy if default behavior isn't desired.
       # Default behavior: latest images will be Always else IfNotPresent.
       pullPolicy: IfNotPresent
       # List of secrets in the same namespace of the component that can be used to pull images from private repositories.
       pullSecrets: []
 
+serviceType: managed
+
 podDisruptionBudget:
   minAvailable: 0
   # maxUnavailable: 1
 
 deployment:
-  annotations: {}
   envoyGateway:
     image:
       # if both this and global.imageRegistry are specified, this has to include both registry and repository explicitly, eg docker.io/envoyproxy/gateway
@@ -54,6 +56,7 @@ deployment:
       runAsNonRoot: true
       runAsGroup: 65532
       runAsUser: 65532
+      readOnlyRootFilesystem: true
       seccompProfile:
         type: RuntimeDefault
   ports:
@@ -109,7 +112,6 @@ createNamespace: false
 
 kubernetesClusterDomain: cluster.local
 
-# -- Certgen is used to generate the certificates required by EnvoyGateway. If you want to construct a custom certificate, you can generate a custom certificate through Cert-Manager before installing EnvoyGateway. Certgen will not overwrite the custom certificate. Please do not manually modify `values.yaml` to disable certgen, it may cause EnvoyGateway OIDC,OAuth2,etc. to not work as expected.
 certgen:
   job:
     annotations: {}
@@ -117,7 +119,12 @@ certgen:
     pod:
       annotations: {}
       labels: {}
-    resources: {}
+    resources:
+      requests:
+        cpu: 50m
+        memory: 100Mi
+      limits:
+        memory: 500Mi
     affinity: {}
     tolerations: []
     nodeSelector: {}
@@ -141,3 +148,15 @@ certgen:
 topologyInjector:
   enabled: true
   annotations: {}
+
+kubectlApplyJob:
+  enabled: true
+  files:
+  - files/generated/gateway.envoyproxy.io_backends.yaml
+  - files/generated/gateway.envoyproxy.io_backendtrafficpolicies.yaml
+  - files/generated/gateway.envoyproxy.io_clienttrafficpolicies.yaml
+  - files/generated/gateway.envoyproxy.io_envoyextensionpolicies.yaml
+  - files/generated/gateway.envoyproxy.io_envoypatchpolicies.yaml
+  - files/generated/gateway.envoyproxy.io_envoyproxies.yaml
+  - files/generated/gateway.envoyproxy.io_httproutefilters.yaml
+  - files/generated/gateway.envoyproxy.io_securitypolicies.yaml
